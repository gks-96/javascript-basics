let js = 'amazing';

let country = 'india';
let continent = 'Asia'
let population = 1000000;

console.log( country  + " " + continent + " " + population);


// if value is not defined, it is stored as null
// is variable is undefined, then its type will also be undefined
let children
console.log(children)

/*
Js is dynamically typed - we do not have to manually define the data type of the value
, data types are defined automatically
 */

// to get type of some variable
console.log(typeof true);

// for string, we can use both single quote and double quote